Create a local directory called second-repo
cd into the second-repo directory
git init
open vscode with 'code .'
create a notes.txt file and take note of the letter u - represents untracked

git status - check the status of our local repo
git add to track
git commit to add repo
git log 
git remote  add origin
git remote -v to fetch and push to github
git branch to put in main branch
git push to github
